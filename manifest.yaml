name: hello-tool
version: 0.1.0
description: Эталонный плагин berimor — приветствие как инструмент
capabilities:
  tools:
    - name: hello.greet
      description: Печатает приветствие (аргумент name — необязательный)
      mutates: false
